buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.13'
    }
}
apply plugin: 'android-library'

android {
    compileSdkVersion 'android-15'
    buildToolsVersion '23.0.2'

    buildTypes {
        release {
            runProguard false
            proguardFile getDefaultProguardFile('proguard-android.txt')
        }
    }
}
